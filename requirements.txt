pytube>=12.1.0
moviepy>=1.0.3
pandas>=2.0.0
openpyxl>=3.1.0 
yt-dlp>=2024.04.09
genai>=0.1.0
google-cloud-aiplatform

# FFmpeg é necessário para cortar e mesclar vídeos:
# Instale via conda: conda install -c conda-forge ffmpeg
# Ou via sistema: sudo apt install ffmpeg / brew install ffmpeg / choco install ffmpeg